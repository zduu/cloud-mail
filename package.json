{
  "name": "cloud-mail-workspace",
  "private": true,
  "packageManager": "pnpm@10.11.1",
  "scripts": {
    "build": "pnpm --dir mail-vue run build",
    "test": "pnpm --dir mail-worker exec vitest run",
    "deploy": "pnpm --dir mail-worker exec wrangler deploy"
  }
}
